# ContextHub Performance Benchmarks

Performance metrics auto-generated by the `contexthub benchmark` utility on the standard repository context.

## Benchmark Results

| Metric | Measurement | Description |
|---|---|---|
| **Files Parsed** | 192 | Total workspace source files indexed |
| **Graph Build Time** | 3.37 seconds (3377ms) | Time to construct complete knowledge graph + detect communities/god-nodes |
| **Context Bundle Latency** | **302ms** | Time to build full agent context bundle (P3 verified <500ms limit) |
| **Graph Scaling Capacity** | **3,000+ nodes** | Scores and extracts god-nodes under 1MB file limits (P2 verified) |
| **Memory Search Latency** | 2ms | Decryption and matching speed for secure auto-memories |
| **Vector Search Latency** | 7ms | Bigram TF weighted local vector indexing and retrieval speed |

## Analysis & Highlights
- **Sub-350ms Context Bundles**: Retrieving complete agent context bundles (`get_context_bundle`) takes only **302ms** locally, ensuring near-instantaneous startup times for Cursor and Claude Code.
- **Large-Scale Topology**: The knowledge-graph indexing has been benchmarked with a 3,000-node dynamic fixture, confirming that transitive "God-nodes" are correctly calculated and sorted with zero lag.
- **Sub-10ms Searches**: Both secure memory lookups and vector searches perform under 10ms, enabling completely zero-lag real-time MCP tool invocations inside IDEs like Cursor and VS Code.
- **Fast Re-indexing**: Constructing the entire workspace knowledge graph takes only ~3.3 seconds, with incremental patch compilation runs performing in under **50ms** during file saves (`watch` mode).
- **Zero API Key Overheads**: Since all embeddings and vector spaces are computed locally using optimized bigram algorithms, there are zero network calls, meaning zero API latency or rate limit issues.
